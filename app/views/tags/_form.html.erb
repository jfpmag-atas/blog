<%= form_for @tag, url: new_tag_path do |g| %>
	<table>
		<tr>
			<td>
				<%= g.text_field :name %>
				<%= g.hidden_field :article_id, :value => @article.id %>
			</td>
		</tr>
		<tr>
			<td align="right">
				<%= g.submit "Add" %>
			</td>
		</tr>
	</table>
<% end %>